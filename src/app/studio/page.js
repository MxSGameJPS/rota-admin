import { getAIContract } from '@/schemas/contracts';
import styles from '@/app/section.module.css';
export default function StudioPage() { const contracts = { npc: getAIContract('npc'), case: getAIContract('case'), item: getAIContract('item') }; return <div className={styles.page}><div className={styles.header}><div><h2>AI Content Studio</h2><p>O Rota controla o formato. OmniRoute, OpenAI, Gemini, Claude ou qualquer provider recebe o JSON Schema oficial e devolve conteúdo compatível.</p></div></div><section className={styles.cards}>{Object.entries(contracts).map(([type, contract]) => <article className={styles.card} key={type}><h4>{type.toUpperCase()}</h4><p>{contract.instructions}</p></article>)}</section><section className={styles.panel}><h3>Contratos reais enviados ao provider</h3><pre className={styles.code}>{JSON.stringify(contracts, null, 2)}</pre></section></div>; }
