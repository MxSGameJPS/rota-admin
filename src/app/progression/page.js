import { listSettings } from '@/services/adminRepository';
import { saveSettingAction } from '@/app/actions/content';
import styles from '@/app/section.module.css';
export default async function ProgressionPage({ searchParams }) { const params = await searchParams; const settings = await listSettings(); return <div className={styles.page}><div className={styles.header}><div><h2>Progressão & Configurações</h2><p>Parâmetros que precisam mudar sem nova build podem ser publicados em `game_settings` e consumidos pelo jogo.</p></div></div>{params?.saved && <div className={styles.notice}>Configuração publicada.</div>}{params?.error && <div className={styles.error}>{params.error}</div>}<section className={styles.panel}><h3>Publicar setting</h3><form className={styles.form} action={saveSettingAction}><div className={styles.formGrid}><input name="key" placeholder="career.progression.v1" required/><input name="description" placeholder="Descrição"/></div><textarea name="value" defaultValue={'{\n  "enabled": true\n}'} required/><button className={styles.primary}>Salvar setting público</button></form></section><section className={styles.panel}><h3>Settings publicados</h3>{settings.length ? <pre className={styles.code}>{JSON.stringify(settings, null, 2)}</pre> : <div className={styles.empty}>Nenhum setting ainda.</div>}</section></div>; }
