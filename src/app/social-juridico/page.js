import { listFeatures } from '@/services/adminRepository';
import styles from '@/app/section.module.css';
export default async function SocialJuridicoPage() { const features = await listFeatures(); return <div className={styles.page}><div className={styles.header}><div><h2>Social Jurídico In-Game</h2><p>Mecânicas simuladas dentro do jogo: blindagem de provas, assinatura digital, notificação extrajudicial, CRM e futuras ferramentas. CTAs institucionais continuam podendo levar ao Social Jurídico real.</p></div></div><section className={styles.panel}><h3>Features publicadas</h3>{features.length ? <div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Feature</th><th>Status</th><th>Ativa</th></tr></thead><tbody>{features.map(f => <tr key={f.id}><td><strong>{f.name}</strong><br/>{f.description}</td><td>{f.status}</td><td>{f.is_active ? 'Sim' : 'Não'}</td></tr>)}</tbody></table></div> : <div className={styles.empty}>Aguardando migration e configuração das features.</div>}</section></div>; }
