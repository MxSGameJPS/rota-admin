import { listFeatures } from '@/services/adminRepository';
import { activateFeatureAction } from '@/app/actions/content';
import StatusBadge from '@/components/StatusBadge/StatusBadge';
import styles from '@/app/section.module.css';
export default async function SocialJuridicoPage({ searchParams }) { const params = await searchParams; const features = await listFeatures(); return <div className={styles.page}><div className={styles.header}><div><h2>Social Jurídico In-Game</h2><p>Mecânicas simuladas dentro do jogo: blindagem de provas, assinatura digital, notificação extrajudicial e CRM. CTAs institucionais podem continuar levando ao Social Jurídico real.</p></div></div>{params?.published && <div className={styles.notice}>Feature ativada e publicada.</div>}{params?.error && <div className={styles.error}>{params.error}</div>}<section className={styles.panel}><h3>Features</h3>{features.length ? <div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Feature</th><th>Status</th><th>Ativa</th><th>Ação</th></tr></thead><tbody>{features.map(f => <tr key={f.id}><td><strong>{f.name}</strong><br/>{f.description}</td><td><StatusBadge status={f.status}/></td><td>{f.is_active ? 'Sim' : 'Não'}</td><td>{f.status !== 'published' || !f.is_active ? <form action={activateFeatureAction}><input type="hidden" name="id" value={f.id}/><button className={styles.secondary}>Publicar e ativar</button></form> : '—'}</td></tr>)}</tbody></table></div> : <div className={styles.empty}>Aguardando migration administrativa.</div>}</section></div>; }
